# --- Database (Neon serverless Postgres, or local docker-compose) ---
DATABASE_URL=postgres://nestava:nestava@localhost:5432/nestava

# --- Auth (Clerk) ---  https://dashboard.clerk.com
CLERK_SECRET_KEY=sk_test_xxx
CLERK_PUBLISHABLE_KEY=pk_test_xxx

# --- Billing (Stripe) --- https://dashboard.stripe.com
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# --- External data vendors (Layer 3 — swap mocks to live) ---
ATTOM_API_KEY=
HOUSECANARY_API_KEY=
FEMA_NFHL=                 # FEMA NFHL is public; no key
GREATSCHOOLS_API_KEY=
BATCHDATA_API_KEY=         # skip trace
BRIDGE_MLS_TOKEN=          # IDX feed (per-MLS approval)
PORT=8787
